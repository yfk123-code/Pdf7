{
  "name": "gemini-ocr-netlify",
  "version": "1.0.0",
  "description": "An OCR website using Gemini API hosted on Netlify",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@google/generative-ai": "^0.15.0"
  }
}
